import { FC } from 'react';
import Layout from '../../components/layout';

const Profile: FC = () => {
    return (
        <Layout>
            <div>Profile</div>
        </Layout>
    );
};

export default Profile;
