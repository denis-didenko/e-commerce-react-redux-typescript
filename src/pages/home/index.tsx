import { FC } from 'react';
import Layout from '../../components/layout';

const Home: FC = () => {
    return (
        <Layout>
            <div>Home</div>
        </Layout>
    );
};

export default Home;
