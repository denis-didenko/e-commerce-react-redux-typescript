import { FC } from 'react';
import Layout from '../../components/layout';

const Cart: FC = () => {
    return (
        <Layout>
            <div>Cart</div>
        </Layout>
    );
};

export default Cart;
